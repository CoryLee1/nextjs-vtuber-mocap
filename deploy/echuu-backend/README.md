# Echuu 后端部署副本

供 Railway 等从主仓库构建时使用，不依赖 echuu-agent 子模块。

**后端逻辑更新后**需同步到此处，再提交主仓库：

- `echuu-agent/public` → `deploy/echuu-backend/public`
- `echuu-agent/workflow/backend` → `deploy/echuu-backend/backend`
- `echuu-agent/requirements.txt` → `deploy/echuu-backend/requirements.txt`
